case_id: C025
title: "Postgres 连接池调优(假近邻,与 C026)"
tags: ["s5-false-neighbor", "kind-pattern", "mixed-zh-en", "group-pg-aspects"]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: pg-aspects

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["连接池", "connection pool", "pool_size", "PgBouncer"]
  - ["Postgres", "postgres"]
consolidation_relations:
  - with_case: C026
    relation: false_neighbor
    expected_action: no_action
    reason: "同实体 Postgres、不同关切:连接池容量调优 vs 全文检索建模;排查连接耗尽时召回 tsvector 全文检索是噪声、二者不构成同一主题互补画面 → no_action(非 complement)"
