case_id: C004
title: "CORS preflight 失败 — auth 中间件先于 CORS / 缺 Allow-Headers(纯英文)"
description: >
  Browser-only 401、curl 正常的经典 CORS preflight 问题:OPTIONS 预检先撞上 auth 中间件被 401,
  且响应的 Access-Control-Allow-Headers 没列 Authorization。修法:OPTIONS 在 auth 前短路 + 补 Authorization 头。
  纯英文 case,覆盖 pure-en。考点:failure 三要素 + 在英文 HTTP 头转储里定位。
tags: [single-thread, kind-failure, tool-call-output-pairs, pure-en]
multilingual: en_only
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 14
  kept_line_nos: [4, 7, 9, 10, 11]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 12, 13, 14]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1
  knowledge_points:
    - kind: failure
      summary_must_contain_any: ["CORS", "preflight", "OPTIONS", "401"]
      summary_must_not_contain: []
      detail_must_mention_all:
        - ["Authorization", "Allow-Headers"]
        - ["preflight", "OPTIONS"]
      detail_must_not_mention: []
