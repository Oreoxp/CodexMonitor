case_id: C017
title: "发布用蓝绿部署(矛盾对,与 C018)"
tags: ["s5-contradiction", "kind-decision", "pure-zh", "group-deploy"]
multilingual: zh_only
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: deploy
created_at_offset_hours: 0

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["蓝绿", "blue-green"]
  - ["回滚", "秒级", "切回", "rollback"]
# 禁词:任何 KP 的 summary/detail 都不得出现(防幻觉 / 证关系成员措辞干净)
expected_distill_case_level_forbidden: ["滚动部署", "rolling"]
consolidation_relations:
  - with_case: C018
    relation: contradiction
    expected_action: keep_both_contradicts
    reason: "同 scope(发布策略)反答案:C017=蓝绿(秒级回滚) vs C018=滚动(省资源);co-temporal、无替换语言 → 留双方互标"
