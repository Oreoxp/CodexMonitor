case_id: C019
title: "消息队列用 RabbitMQ(被 C020 取代,较早)"
tags: ["s5-supersede", "kind-decision", "mixed-zh-en", "group-messaging"]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: messaging
created_at_offset_hours: -72

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["RabbitMQ"]
  - ["消息队列", "message queue", "MQ", "异步"]
consolidation_relations:
  - with_case: C020
    relation: supersede
    expected_action: superseded_by_newer
    reason: "C020 以替换内容(吞吐/持久化重放/分区)从 RabbitMQ 换到 Kafka,且 ts 更晚 → C019.superseded_by=C020"
  - with_case: C023
    relation: false_neighbor
    expected_action: no_action
    reason: "组内审计:C019(RabbitMQ,-72h)与 C023(REST,-96h)同属基础设施选型、向量近,但 MQ vs RPC 不同机制、互不替换 → no_action。"
  - with_case: C024
    relation: false_neighbor
    expected_action: no_action
    reason: "组内审计:C019(RabbitMQ)与 C024(gRPC,0h)不同主题;C024 更新但替换的是 REST 而非 RabbitMQ → S5 不得仅凭新近判 supersede,no_action。"
