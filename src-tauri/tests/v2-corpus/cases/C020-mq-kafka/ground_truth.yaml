case_id: C020
title: "消息队列从 RabbitMQ 换到 Kafka(取代 C019)"
tags: ["s5-supersede", "kind-decision", "mixed-zh-en", "group-messaging"]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: messaging
created_at_offset_hours: 0

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["Kafka"]
  - ["RabbitMQ"]
  - ["换到", "迁到", "改用", "替", "替代", "迁移", "switch", "migrate", "migrated", "moved", "replace", "from RabbitMQ"]
consolidation_relations:
  - with_case: C019
    relation: supersede
    expected_action: supersedes_older
    reason: "本条以替换内容取代 C019(从 RabbitMQ→Kafka),ts 更晚 → C019.superseded_by=C020"
  - with_case: C023
    relation: false_neighbor
    expected_action: no_action
    reason: "组内审计·跨时间陷阱:C020(Kafka,0h 较新)与 C023(REST,-96h 较旧)主题不同(MQ vs 传输协议),C020 不替换 C023 → S5 必须有替换内容才判 supersede,仅凭‘更新’不行 → no_action。"
  - with_case: C024
    relation: false_neighbor
    expected_action: no_action
    reason: "组内审计:C020(Kafka,0h)与 C024(gRPC,0h)co-temporal 但不同主题(MQ vs RPC);并存、互不取代 → no_action。"
