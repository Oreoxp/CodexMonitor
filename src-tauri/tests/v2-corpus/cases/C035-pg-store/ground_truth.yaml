case_id: C035
title: "主数据库用 Postgres(互补对,与 C036)"
tags: ["s5-complement", "kind-decision", "pure-zh", "group-pg-store"]
multilingual: zh_only
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: pg-store

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["Postgres", "postgres"]
  - ["主数据库", "主存储", "主库", "核心数据库", "数据库", "primary", "main database"]
consolidation_relations:
  - with_case: C036
    relation: complement
    expected_action: cluster_keep_both
    reason: "同主题(Postgres 使用):C035=用 PG 的决策 + C036=PG 权限角色安全约定;一起构成操作 Postgres 的完整画面、都该留并成簇 → complement(标 cluster、不合并)"
