case_id: C006
title: "AcmeQueue 的 /healthz 是 readiness、/livez 是 liveness(fact + emoji)"
description: >
  k8s 反复重启,根因是探针指反。可复用的事实:/livez=liveness(进程活着就 200),
  /healthz=readiness(DB 连接池建好才 200,否则 503)。含中文 + emoji(🤔 ✅),覆盖 utf8-multibyte。
  detail 的硬断言只锁 /healthz readiness 主事实,/livez 放进 summary 的 OR 集以降脆性
  (LLM 可能拆成两条 fact KP)。
tags: [single-thread, kind-fact, tool-call-output-pairs, utf8-multibyte, mixed-zh-en]
multilingual: mixed
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 14
  kept_line_nos: [4, 7, 9, 10, 11]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 12, 13, 14]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例(≥1 KP);条数不再断言
  knowledge_points: []   # 移除 kind 断言:该知识可合法归为 fact/failure/decision;改用 case 级内容断言
# case 级断言:跨该 case 全部 KP(kind 无关),每组至少命中一个
expected_distill_case_level:
  - ["/healthz", "healthz"]
  - ["readiness", "就绪", "DB", "连接池", "503"]
  - ["/livez", "livez", "liveness"]
