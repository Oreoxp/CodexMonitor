case_id: C029
title: "Kafka 消费者手动提交 offset 防丢消息(假近邻,与 C030)"
tags: ["s5-false-neighbor", "kind-pattern", "mixed-zh-en", "group-kafka-aspects"]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: kafka-aspects

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["offset", "手动提交", "commitSync", "auto.commit"]
  - ["Kafka", "消费", "consumer"]
consolidation_relations:
  - with_case: C030
    relation: false_neighbor
    expected_action: no_action
    reason: "同实体 Kafka 但分属应用层(消费 offset 语义)与基础设施层(broker 磁盘文件系统)两个正交关注点;排查丢消息时召回‘用 XFS’是噪声、不构成同一主题互补画面 → no_action(非 complement)"
