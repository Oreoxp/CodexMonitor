case_id: C036
title: "Postgres app_rw 只授 DML、DDL 走 migrator(互补对,与 C035)"
tags: ["s5-complement", "kind-pattern", "mixed-zh-en", "group-pg-store"]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: pg-store

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 10
  kept_line_nos: [4, 7]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 10]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1   # 正例路由;条数不断言
  knowledge_points: []
# case 级:跨该 case 全部 KP,每组至少命中一个(kind 无关)
expected_distill_case_level:
  - ["app_rw", "DML", "DDL"]
  - ["迁移", "migrator", "schema", "权限"]
consolidation_relations:
  - with_case: C035
    relation: complement
    expected_action: cluster_keep_both
    reason: "同主题 Postgres 的另一半(安全角色约定);与 C035 决策互补、成簇"
