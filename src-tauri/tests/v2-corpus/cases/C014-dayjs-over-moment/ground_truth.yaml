case_id: C014
title: "前端时间库选 day.js 替 moment(decision,中英混排)"
description: >
  因 moment 已 deprecated(维护模式)、体积大且不支持 tree-shaking,改用 API 兼容、~2KB 的 day.js。
  summary 很可能写成 'chose day.js over moment',故 moment 不放进 must_not_contain;
  反向断言只挡未讨论过的库(luxon / date-fns)以防幻觉。也是 C001 cross-topic 搜索的预期命中目标。
tags: [single-thread, kind-decision, mixed-zh-en]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 16
  kept_line_nos: [4, 7, 10, 13]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 9, 11, 12, 14, 15, 16]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1
  knowledge_points:
    - kind: decision
      summary_must_contain_any: ["day.js", "dayjs", "Day.js"]
      summary_must_not_contain: ["luxon", "date-fns"]
      detail_must_mention_all:
        - ["deprecated", "维护模式", "停止维护", "不维护"]
        - ["体积", "bundle", "2KB", "tree-shaking", "大"]
      detail_must_not_mention: ["luxon", "date-fns"]
