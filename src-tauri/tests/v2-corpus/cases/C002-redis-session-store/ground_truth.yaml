case_id: C002
title: "会话存储选型 — 定 Redis(单段、与 C001 矛盾)"
description: >
  另一条对话对同一个问题(AcmeChat 会话状态存哪)给出相反结论:因为读写在热路径、要低延迟,
  且希望过期会话用 TTL 自动清理,所以定 Redis。transcript 里不出现 Postgres,
  保证 summary 反向断言干净。与 C001 构成 contradiction 对。
tags: [single-thread, kind-decision, mixed-keep-drop, mixed-zh-en]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures
group: session

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 20
  kept_line_nos: [4, 7, 9, 14, 17]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 10, 11, 12, 13, 15, 16, 18, 19, 20]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1
  knowledge_points:
    - kind: decision
      summary_must_contain_any: ["Redis", "redis"]
      summary_must_not_contain: ["Postgres", "PG", "MongoDB"]
      detail_must_mention_all:
        - ["低延迟", "热路径", "快", "latency", "共享", "跨实例"]
        - ["TTL", "过期", "expire", "自动清"]
      detail_must_not_mention: ["Postgres"]

consolidation_relations:
  - with_case: C001
    relation: contradiction
    expected_action: keep_both_contradicts
    reason: "同 scope 反答案的另一半:C002=Redis。与 C001=Postgres 互为矛盾,二者都保留。"
