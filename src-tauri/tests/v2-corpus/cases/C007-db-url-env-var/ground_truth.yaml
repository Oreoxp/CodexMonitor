case_id: C007
title: "acme-api 的 Postgres 连接串来自 env ACME_DB_URL(fact,藏在配置转储里)"
description: >
  问"连接串从哪配",--print-config 输出一大段配置项,目标事实(db.url <- env ACME_DB_URL,
  unset 时回退 postgres://localhost:5432/acme_dev)埋在中间。可复用事实型知识点。
  考点:noisy-tool-output 中提取一个具体 config 事实,kind=fact(不是 failure,刻意去掉了报错叙事)。
tags: [single-thread, kind-fact, tool-call-output-pairs, noisy-tool-output, mixed-zh-en]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 14
  kept_line_nos: [4, 7, 9, 10, 11]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 12, 13, 14]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1
  knowledge_points:
    - kind: fact
      summary_must_contain_any: ["ACME_DB_URL", "连接串", "环境变量", "db.url", "DSN"]
      summary_must_not_contain: []
      detail_must_mention_all:
        - ["ACME_DB_URL"]
        - ["postgres://localhost:5432/acme_dev", "5432", "默认", "default", "回退"]
      detail_must_not_mention: []
