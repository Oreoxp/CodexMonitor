case_id: C005
title: "本仓库加 Tauri command 的固定套路(pattern)"
description: >
  新增 #[tauri::command] 后前端 invoke 报 'command not found',因为只写了函数定义没注册。
  本仓库约定两处都要改:main.rs 的 generate_handler![...] 宏注册 + bindings.ts 暴露 TS 类型,缺一不可。
  这是 codebase 内可复用的 idiom(不是一次性决策),应蒸馏为 pattern。
tags: [single-thread, kind-pattern, tool-call-output-pairs, mixed-keep-drop, mixed-zh-en]
multilingual: zh_majority
thread_count: 1
segment_count: 1
agent_id: test-agent-001
team_id: team-fixtures
project_hash: hash-fixtures

# >>> expected_ingest (generated by corpus_tool.py — run `verify --fix`) >>>
expected_ingest:
  raw_event_count: 20
  kept_line_nos: [4, 7, 9, 10, 11, 14, 17]
  dropped_line_nos: [1, 2, 3, 5, 6, 8, 12, 13, 15, 16, 18, 19, 20]
  boundary_line_nos: []
# <<< expected_ingest <<<

expected_distill:
  expected_kp_count: 1
  knowledge_points:
    - kind: pattern
      summary_must_contain_any: ["command", "注册", "register", "generate_handler", "bindings", "两处", "两个地方"]
      summary_must_not_contain: []
      detail_must_mention_all:
        - ["generate_handler", "handler", "invoke_handler", "main.rs"]
        - ["bindings.ts", "bindings", "TS 类型", "类型"]
      detail_must_not_mention: []
